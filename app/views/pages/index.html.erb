<h1>Pages</h1>
<p><%= link_to "New Post-it" , new_page_path %></p>

<% @pages.each do |page| %>
  <h2><%= page.title %><h2>
  <%= link_to "Show Post-it", page_path(page) %>
  <%= link_to "Edit", edit_page_path(page.id) %>
  <%= link_to "Delete", page_path(page.id), method: :delete %>
  <% end %> 
